Polkadot's Async Backing reduces block time to ~6 seconds (vs 12s prior).

- **Faster liquidations:** AI Guardian can detect and execute `flashRepay` within one block.
- **Yield routing:** XCM execution benefits from quicker finality.
- **Pitch angle:** "Sub-6-second rebalance" — competitive edge for vault safety.