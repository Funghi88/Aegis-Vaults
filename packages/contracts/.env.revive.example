# Revive deploy — copy to .env.revive (in packages/contracts/), fill values, then run:
#   npm run deploy:revive
# The script loads .env.revive automatically. Do not commit .env.revive.

# Local or remote Revive node. Local build needs 50+ GB (polkadot-sdk). Or use a cloud VM and set its ws URL.
WS_ENDPOINT=ws://127.0.0.1:9944

# 12 or 24 word BIP39 mnemonic — deployer pays gas
DEPLOYER_SEED=your twelve or twenty four word mnemonic goes here

# EVM address (0x...) for flashRepay — from docs/contracts.md or your deployer
GUARDIAN_ADDRESS=0x0000000000000000000000000000000000000001
