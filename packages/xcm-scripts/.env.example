# Paseo Asset Hub. Script auto-adds fallbacks if one fails.
# Use IBP first (often more reliable than polkadot.io for testnet).
WS_ENDPOINT=wss://sys.ibp.network/asset-hub-paseo
# Or explicit list: WS_ENDPOINTS=wss://sys.ibp.network/asset-hub-paseo,wss://asset-hub-paseo-rpc.polkadot.io

RECIPIENT_SS58=
SENDER_SEED=
DEST_PARA_ID=4001
ASSET_ID=native
AMOUNT=100000000
