# Vault contract address (from deploy output). Leave empty to use default.
VITE_VAULT_ADDRESS=0xa44639cd0d0e6c6607491088c9c549e184456122

# Vault type: "full" = AegisVault (repay, withdraw); "demo" = AegisVaultDemo (no repay/withdraw)
VITE_VAULT_TYPE=demo

# Blockchain RPC URL — NOT the frontend dev server (localhost:5173).
VITE_RPC_URL=https://services.polkadothub-rpc.com/testnet

# Chain ID for MetaMask. Hardhat local: 420420417
VITE_CHAIN_ID=420420417

# Native token symbol. PAS for Paseo/Polkadot Hub TestNet, DEV for local Hardhat
VITE_NATIVE_TOKEN=PAS

# WebSocket endpoint(s) for XCM. Comma-separated = try in order if one fails.
VITE_WS_ENDPOINTS=wss://sys.ibp.network/asset-hub-paseo,wss://asset-hub-paseo-rpc.polkadot.io
